# Public-domain catalog

`public-domain-books.json` contains only books that have passed the StoryPals import workflow.

Do not populate this file by blindly crawling book websites. Import text only after verifying that the specific work and edition may be redistributed and transformed for your intended territory.

The Phase 3 CLI accepts a local UTF-8 text file and records rights metadata with the normalized StoryPals book.
